export const initHomePage = () => {
    // Espaço reservado para comportamentos específicos da home (ex: animações customizadas).
    // Mantido simples para cumprir responsabilidade única e facilitar futuras extensões.
};
